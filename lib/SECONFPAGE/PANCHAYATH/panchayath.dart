import 'package:flutter/material.dart';

class panchayathscreen extends StatelessWidget {
  const panchayathscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("PANCHAYATH INFORMATIONS"),),
    );
  }
}
